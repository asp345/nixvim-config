{
  plugins.noice = {
    enable = true;
    autoLoad = true;
    settings = {
      messages = {
        enabled = false;
      };
      notify = {
        enabled = false;
      };
      lsp = {
        message = {
          enabled = false;
        };
        progress = {
          enabled = false;
        };
      };
    };
  };
}
